class VideoModel {
  final String title;
  final String thumbnail;
  final String url; // Instagram or any video link (future use)

  VideoModel({
    required this.title,
    required this.thumbnail,
    required this.url,
  });
}
